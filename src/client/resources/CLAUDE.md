# resources/ (client source set)

Client-source-set mixin configuration.

## Files

| File                             | What                                                                                            | When to read                                    |
| -------------------------------- | ----------------------------------------------------------------------------------------------- | ----------------------------------------------- |
| `fractalterrain.client.mixins.json` | Client mixin registration (package `…fractal_terrain.mixin.client`); the `client` list is empty | Registering a client-side mixin, changing client mixin config |
