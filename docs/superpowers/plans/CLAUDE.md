# plans/

## Files

| File                                    | What                                                                                  | When to read                                                          |
| --------------------------------------- | -------------------------------------------------------------------------------------- | --------------------------------------------------------------------- |
| `2026-08-24-heightmap-slice-sampling.md` | Task-by-task plan for the per-chunk tensor slice fill and the relief/biome cache budgets; specs its bit-identical acceptance bar | Executing the slice-sampling change, or checking the constraints it commits to |
| `2026-09-01-conventions-alignment.md`    | 15 tasks for the fastutil migration, the `profile ↔ providers` cycle split, and the `BiomeProvider`/`ml.models` injection seams; partitioned into a byte-identical Group A and an output-affecting Group B gated on order-independent invariants | Executing the collection migration, or checking the measured 2026-09-01 test baseline and the nine corrections it makes to its spec |
| `2026-09-02-radial-primitive-carve.md`   | 6 tasks adding the `RadialPrimitive` family (`ConfluencePrimitive`, `SourcePrimitive` promoted into it) and the second radial pass in `computeRiverGrid`; carries the three merge properties the pass rests on, the `local_river_units_v3` store rename, and the collision with the surface-painting plan over `buffers.dist` | Executing the radial carve, or checking why the radial pass reseeds `dist` and why that conflicts with publishing `Types.RIVER_DIST` |
| `2026-09-02-hydrology-surface-painting.md` | 4 tasks banding the carve footprint scale, publishing it as `Types.RIVER_DIST`, and letting a `HydrologyProfile` pick the blocks its own carve exposes; corrects its spec's golden-re-baseline claim and adds the control-point clamps the spec omits | Executing the surface-painting change, or checking why `carvePrimitive` bands its coordinate and why no golden may move |
