# specs/

## Files

| File                                          | What                                                                                             | When to read                                                                     |
| --------------------------------------------- | ------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------- |
| `2026-08-19-dead-code-survey.md`              | Grep-reachability inventory of `src/main` at `6a7086e`; superseded — acted on in `06a15dd`       | Checking what the dead-code sweep removed, or re-running the same reachability method |
| `2026-08-20-heightmap-slice-sampling-design.md` | Per-chunk tensor slices replacing per-pixel `getValue`, plus relief/biome cache byte budgets; status *proposed* | Understanding why the heightmap fill reads slices, or before changing `ChunkChannelFill`/tile cache limits |
| `2026-09-02-radial-primitive-carve-design.md`  | `RadialPrimitive`/`RadialProfile`: confluences and sources carve a radial bowl in a second bed-carve pass; carries the merge-arithmetic decisions (D4–D6) and the `SourcePrimitive` payload change that forces a primitive-store rename; status *implemented* across `b6fb07f`..`8a46a6f` (`03cd2f5`, inside that range, is a different, concurrent change and not this spec's work) | Before touching the carve merge law, adding a carving feature family, or changing `SourcePrimitive`'s serialized form |
| `2026-09-02-hydrology-surface-painting-design.md` | Banding the carve's footprint scale onto fixed breakpoints so a `HydrologyProfile` can pick the blocks its own carve exposes; the `SurfaceMaterial` token seam keeping `hydrology/` free of `net.minecraft`; status *proposed*, planned in `plans/2026-09-02-hydrology-surface-painting.md`, which corrects its golden-re-baseline claim | Before changing what blocks a river places, the banded `d` coordinate, or the `Types.RIVER_DIST` channel |
| `2026-09-03-river-network-history-design.md`  | Replaces `RiverNetwork`'s write-only `previousStates` and never-emitted `removedPaths` with one `ArrayDeque<HydrologicalPrimitive>`; adds `byte time()`, the `HistoricPrimitive` layout, and the deferred elevation/influence seam; status *proposed*, history stays disabled | Before touching `RiverNetwork` history, the oxbow/abandoned primitive payloads, or wiring `ChannelElevationAssigner` to shed primitives |
| `2026-09-03-rivernetwork-spatial-index-design.md` | Removes `RiverNetwork`'s mutable `QuadTree`: `detectCrossings` moves to `ImmutableRTree` (no new class, an existing build-once/query-many fit), `manageCutoffs` moves to a new mutable `SpatialHashGrid` (needs live removal `ImmutableRTree` can't do); `AtomicView`'s sweep-line stays untouched; status *proposed* | Before touching `RiverNetwork`'s spatial-index usage, adding a `math/ds/` index, or changing `detectCrossings`/`manageCutoffs` |
| `2026-09-20-carve-dispatch-design.md`         | Finishes `8bf6885`'s non-compiling carve refactor: both passes dispatch on the primitive (`carveBed`, `carveInfluence`) instead of an `instanceof` chain and an enum, with LUT construction split off as its own per-primitive hook; `BedCarver`/`InfluenceCarver`/`LatticeCarve` extraction and renames; and one shared `dist[]` merge law (banded radial distance, no radial `typeMask` claim, one height rule); extracted from `2026-09-17-primitive-grid-storage-design.md`'s Part 2 to land without the storage rewrite; status *implemented* across `d4b0274`..`ffa198f`, plus the documentation move landing this spec's own record | Before touching bed/shell carve dispatch, the `LatticeCarve` buffers, or `Types.RIVER_DIST`'s meaning |
| `2026-09-17-primitive-grid-storage-design.md` | Replaces `RiverProvider`'s per-tile `ImmutableRTree` with a chunk-aligned CSR `PrimitiveGrid`; its Part 2 is superseded by `2026-09-20-carve-dispatch-design.md`, and its Part 1 halo is derived from `MAX_INFLUENCE_RADIUS` rather than a `RiverPrimitive`'s actual `1.5 × influence` rectangle half-width; status: Part 2 *implemented* via `2026-09-20-carve-dispatch-design.md`, Part 1 still *proposed* and needs a revision pass | Before changing how primitives are stored or queried per tile — read the carve-dispatch spec first for anything about the carve itself |
| `2026-09-01-conventions-alignment-design.md`  | fastutil migration (162 sites), injection seams, `profile`↔`providers` cycle, dead code; partitioned by whether an item can change terrain; status *implemented* in `7bd587e`, with the `ml/models` caller migration and `OnnxModel`'s feed map left out of scope | Before migrating a collection, touching the service locator, or changing anything whose iteration order feeds generation |
